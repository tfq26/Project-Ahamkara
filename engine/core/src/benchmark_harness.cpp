#include "ae/core/benchmark_harness.h"

// BenchmarkHarness is primarily header-only; this file exists for
// future non-inline extensions and to ensure the header compiles standalone.
// All core functionality (BenchmarkTimer, BenchmarkStats, BenchmarkRunner)
// is implemented inline in the header to support the benchmark/test style
// used in this project (single-file tests without separate compilation).

namespace ae {
// Placeholder for any future out-of-line functions.
} // namespace ae
