#include "ae/core/budget_config.h"

namespace ae {
// BudgetConfig is primarily header-only. This compilation unit exists to
// ensure the header compiles standalone and as an anchor for future
// non-inline extensions (e.g. baseline file I/O).
} // namespace ae
