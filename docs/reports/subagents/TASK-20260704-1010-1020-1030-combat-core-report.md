---
type: subagent-report
category: gameplay-combat
status: implemented
created: 2026-07-05
agent: oz
subsystems: [game, client, engine/ui]
branch: agent/phase3/combat-core
validation: [cmake --build --preset debug, ./scripts/run-tests.sh --preset debug]
---

# Subagent Report — Phase 3 Combat Core (Tasks 1010, 1020, 1030)

## Tasks

1. **TASK-20260704-1010-weapon-fire-control** — Normalize fire modes, ammo, reserve, reload, cooldown, and deterministic recoil/spread through the runtime data model.
2. **TASK-20260704-1020-combat-hit-resolution** — Split hitscan/projectile damage resolution from feedback.
3. **TASK-20260704-1030-combat-abilities-core** — Add melee/grenade/class-ability cooldown/energy plumbing as player-owned combat state.

## Status

All three tasks: **implemented**

## Files Changed

### Task 1010 — Weapon Fire Control
- `game/include/ahamkara/game/gameplay_types.h` — Added `rpm`, `reload_time_s`, `reserve_ammo_max` fields to `WeaponDefinition`; added `fire_interval()` method deriving cooldown from RPM
- `game/include/ahamkara/game/weapon_registry.h` — Updated all 3 weapon definitions with RPM, reload time, and reserve ammo max
- `game/src/weapon_runtime.cpp` — `start_reload()` reads `reload_time_s` from definition; `apply_definition()` uses `reserve_ammo_max`
- `game/include/ahamkara/game/world.h` — Added `recoil_index()`, `advance_recoil()`, `reset_recoil()` accessors
- `game/src/world_projectile.cpp` — Removed ad-hoc fire interval constants; fire cooldown now from `def.fire_interval()`; replaced heuristic weapon detection with explicit slot/fire_mode checks; added deterministic recoil helper and `deterministic_spread_offset()` replacing `std::rand()`

### Task 1020 — Combat Hit Resolution
- `game/src/world_projectile.cpp` — Added `resolve_damage_to_dummy()` (centralized armor absorption/health/lethality) and `emit_hit_feedback()` (hitmarker, damage number, audio); both `step_projectiles()` and `fire_hitscan()` now use these helpers instead of duplicated inline logic

### Task 1030 — Combat Abilities Core
- `game/include/ahamkara/game/player.h` — Added `AbilityState` struct with grenade/special/artifact cooldowns, energy pool, ultimate charge; added `tick_abilities()`, `use_grenade()`, `use_special()` methods
- `game/include/ahamkara/game/world.h` — Added ability state accessors delegating to Player
- `game/src/world.cpp` — Added `tick_abilities()` call and `ability_pressed` input handling in `tick_internal()`
- `client/include/ahamkara/client/debug_scene_bridge.h` — Added ability state fields to `ClientSimulationSnapshot`
- `client/include/ahamkara/client/local_play.h` — Added `get_ability_state()` accessor
- `client/src/local_play.cpp` — Implemented `get_ability_state()` delegating to World
- `client/src/threaded_local_runtime.cpp` — Populate ability state in snapshot builder
- `client/src/client_frame_pipeline.cpp` — Wire ability state from snapshot into `HudState`

## Commands Run

```sh
cd /Users/taufeeqali/Projects/ahamkara-phase3
cmake --build build/debug -j$(sysctl -n hw.ncpu)
./scripts/run-tests.sh --preset debug
```

## Validation Results

**Build:** success (incremental, pre-existing entt deprecation warnings only)
**Tests:** 17/17 passed (100%)

## Commit History

1. `92df490` — TASK-20260704-1010: weapon fire control
2. `8559244` — TASK-20260704-1020: combat hit resolution
3. `1c5e6ac` — TASK-20260704-1030: combat abilities core

## Known Gaps

- Recoil pattern values are advanced deterministically but not yet integrated into the camera/look system — that belongs in a future camera integration slice
- Ultimate charge currently builds from `use_grenade()`/`use_special()` but not from weapon damage — can be wired when combat actions need to feed the ultimate meter
- No dedicated unit tests for new ability state (existing tests cover weapon runtime and gameplay types)
- `is_shotgun` detection still uses slot check rather than a dedicated pellet-count field — could be data-driven when more weapon types are added

## Runtime Risks

- **Low**: All changes are additive or refactoring with preserved behavior. Fire intervals match previous constants (400 RPM → 0.15s, 100 RPM → 0.6s, etc.). The damage resolution helpers produce identical results to the inline code they replaced. Ability state defaults are inert (cooldowns at 0, grenades available).

## Cross-Agent Dependencies

- The weapon runtime seam from TASK-1000 is used by all three tasks
- Ability state populates the existing `HudAbilityIcon` HUD elements through the snapshot pipeline
- Expansion tasks (damage feedback wiring, combat-test replay) can build on the resolved feedback path

## Recommended Next Step

Queue expansion tasks: damage feedback wiring (hitmarker/damage number polish) and combat-test replay scenarios.
