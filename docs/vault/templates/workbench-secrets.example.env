# Workbench secrets template
#
# Recommended real file path:
#   /Users/taufeeqali/projects/workbench-vault/.workbench-secrets.env
#
# This example file is safe to keep in the repo because it contains no secrets.
# Do not store real keys in this repository or in the shared vault.

# ── Worker / classifier / bookkeeping (Google) ───────────────────────
GOOGLE_API_KEY=

# ── Primary reviewer (custom OpenAI-compatible endpoint) ─────────────
PRIMARY_REVIEWER_BASE_URL=
PRIMARY_REVIEWER_API_KEY=

# ── Secondary reviewer / escalation fallback (OpenAI) ────────────────
OPENAI_API_KEY=
